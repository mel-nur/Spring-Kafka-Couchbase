package com.melikenur.StreamCart;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class StreamCartApplication {

	public static void main(String[] args) {
		// Uygulama başlatılıyor. Spring Boot, bileşen taraması ve otomatik konfigürasyon yapar.
		// Bu sınıf uygulamanın giriş noktasıdır.
		SpringApplication.run(StreamCartApplication.class, args);
	}

}
